@extends('layouts.app')

@section('title', 'Approval Workflows - ' . config('app.name'))

@section('content')
<div x-data="{ open: false, edit: false, item: {}, deleteId: null }" class="space-y-6">
    <div class="flex items-center justify-between"><div><h2 class="text-2xl font-bold text-gray-900 dark:text-white">Approval Workflows</h2><p class="mt-1 text-sm text-gray-600 dark:text-gray-400">Manage leave approval workflow scopes.</p></div><button @click="open = true; edit = false; item = { employee_ids: [] }" class="px-4 py-2 bg-indigo-600 hover:bg-indigo-700 text-white text-sm font-medium rounded-xl">Add New</button></div>
    @session('success')<div class="p-4 bg-green-50 dark:bg-green-900/30 border border-green-200 dark:border-green-800 rounded-xl text-sm text-green-700 dark:text-green-300">{{ $value }}</div>@endsession
    <x-list-search :action="route('master-data.approval-workflows')" placeholder="Search workflow, description, company, or department" />
    <div class="bg-white dark:bg-gray-800 rounded-2xl border border-gray-200 dark:border-gray-700 overflow-hidden shadow-sm"><table class="w-full text-sm"><thead><tr class="bg-gray-50 dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700"><th class="text-left px-6 py-4 font-semibold text-gray-900 dark:text-white">Name</th><th class="text-left px-6 py-4 font-semibold text-gray-900 dark:text-white">Type</th><th class="text-left px-6 py-4 font-semibold text-gray-900 dark:text-white">Company</th><th class="text-left px-6 py-4 font-semibold text-gray-900 dark:text-white">Department</th><th class="text-right px-6 py-4 font-semibold text-gray-900 dark:text-white">Actions</th></tr></thead><tbody class="divide-y divide-gray-100 dark:divide-gray-700">@forelse($approvalWorkflows as $workflow)<tr><td class="px-6 py-4 text-gray-900 dark:text-white font-medium">{{ $workflow->name }}</td><td class="px-6 py-4 text-gray-600 dark:text-gray-400">{{ $workflow->type ?? '-' }}</td><td class="px-6 py-4 text-gray-600 dark:text-gray-400">{{ $workflow->company?->name ?? 'Any' }}</td><td class="px-6 py-4 text-gray-600 dark:text-gray-400">{{ $workflow->department?->name ?? 'Any' }}</td><td class="px-6 py-4 text-right"><button @click="open = true; edit = true; item = { id: {{ $workflow->id }}, name: @js($workflow->name), description: @js($workflow->description ?? ''), type: @js($workflow->type ?? ''), company_id: @js($workflow->company_id), department_id: @js($workflow->department_id), division_id: @js($workflow->division_id), section_id: @js($workflow->section_id), work_location_id: @js($workflow->work_location_id), employee_ids: @js($workflow->employee_ids ?? []) }" class="text-indigo-600 dark:text-indigo-400 font-medium mr-3">Edit</button><button @click="deleteId = {{ $workflow->id }}" class="text-red-600 dark:text-red-400 font-medium">Delete</button></td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-gray-500 dark:text-gray-400">No workflows found.</td></tr>@endforelse</tbody></table>@if($approvalWorkflows->hasPages())<div class="px-6 py-4 border-t border-gray-200 dark:border-gray-700">{{ $approvalWorkflows->links() }}</div>@endif</div>
    <div x-show="open" x-cloak class="fixed inset-0 z-50 flex items-center justify-center p-4"><div class="fixed inset-0 bg-gray-900/50" @click="open = false"></div><div class="relative bg-white dark:bg-gray-800 rounded-2xl shadow-xl border border-gray-200 dark:border-gray-700 w-full max-w-2xl max-h-[90vh] overflow-y-auto p-6"><h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4" x-text="edit ? 'Edit Workflow' : 'Add Workflow'"></h3><form method="POST" :action="edit ? `/master-data/approval-workflows/${item.id}` : '{{ route('master-data.approval-workflows.store') }}'">@csrf<input type="hidden" name="_method" :value="edit ? 'PUT' : 'POST'"><div class="grid grid-cols-1 md:grid-cols-2 gap-4"><input name="name" x-model="item.name" required placeholder="Name" class="px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white"><input name="type" x-model="item.type" placeholder="Type" class="px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white"><textarea name="description" x-model="item.description" rows="3" placeholder="Description" class="md:col-span-2 px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white"></textarea><select name="company_id" x-model="item.company_id" class="px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white"><option value="">Any company</option>@foreach($companies as $company)<option value="{{ $company->id }}">{{ $company->name }}</option>@endforeach</select><select name="department_id" x-model="item.department_id" class="px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white"><option value="">Any department</option>@foreach($departments as $department)<option value="{{ $department->id }}">{{ $department->name }}</option>@endforeach</select><select name="division_id" x-model="item.division_id" class="px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white"><option value="">Any division</option>@foreach($divisions as $division)<option value="{{ $division->id }}">{{ $division->name }}</option>@endforeach</select><select name="section_id" x-model="item.section_id" class="px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white"><option value="">Any section</option>@foreach($sections as $section)<option value="{{ $section->id }}">{{ $section->name }}</option>@endforeach</select><select name="work_location_id" x-model="item.work_location_id" class="px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white"><option value="">Any location</option>@foreach($workLocations as $location)<option value="{{ $location->id }}">{{ $location->name }}</option>@endforeach</select><x-employee-async-select name="employee_ids" model="item.employee_ids" multiple placeholder="Search and add employees..." class="md:col-span-2" /></div><div class="flex justify-end gap-3 mt-6"><button type="button" @click="open = false" class="px-4 py-2 text-sm bg-gray-100 dark:bg-gray-700 rounded-lg">Cancel</button><button class="px-4 py-2 text-sm text-white bg-indigo-600 rounded-lg" x-text="edit ? 'Update' : 'Save'"></button></div></form></div></div>
    <div x-show="deleteId" x-cloak class="fixed inset-0 z-50 flex items-center justify-center p-4"><div class="fixed inset-0 bg-gray-900/50" @click="deleteId = null"></div><div class="relative bg-white dark:bg-gray-800 rounded-2xl shadow-xl border border-gray-200 dark:border-gray-700 w-full max-w-md p-6 text-center"><h3 class="text-lg font-semibold text-gray-900 dark:text-white">Confirm Delete</h3><form :action="`/master-data/approval-workflows/${deleteId}`" method="POST" class="flex justify-center gap-3 mt-6">@csrf @method('DELETE')<button type="button" @click="deleteId = null" class="px-4 py-2 text-sm bg-gray-100 dark:bg-gray-700 rounded-lg">Cancel</button><button class="px-4 py-2 text-sm text-white bg-red-600 rounded-lg">Delete</button></form></div></div>
</div>
@endsection
